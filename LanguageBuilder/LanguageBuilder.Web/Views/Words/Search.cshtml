@using System.Linq;
@model WordsSearchViewModel

@{
    ViewBag.Title = "Search the words in the dictionaries by language";
}

<h2>@ViewBag.Title</h2>

@Html.Partial("_SearchForm", Model.SearchForm)

<div>
    <cs-alphapager cs-alphabet="ABCDEFGHIJKLMNOPQRSTUVWXYZ"
                   cs-selected-letter="@Model.SelectedLetter"
                   cs-all-label="All"
                   asp-controller="Words"
                   asp-action="Search"
                   cs-selected-letter-param="SelectedLetter"></cs-alphapager>
</div>

@if (Model.Data.Any())
{
    <table class="table table-striped table-bordered">
        <thead>
            <tr>
                <th>Gender</th>
                <th>Content</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var word in Model.Data)
            {
                <tr>
                    <td>@word.Gender</td>
                    <td>@word.Content</td>
                </tr>}
        </tbody>
    </table>
        @*<cs-pager cs-paging-info="@Model.Paging"
                  cs-pagenumber-param="page"
                  asp-controller="Words"
                  asp-action="Search"
                  asp-route-categories="@Model.SearchForm.Languages"
                  asp-route-pagesize="@Model.PageSize"
                  cs-first-page-text="First"
                  cs-last-page-text="Last"
                  cs-previous-page-text="Prev"
                  cs-next-page-text="Next">
        </cs-pager>*@


}
